#include <stdio.h>
#include <stdlib.h>

int     main(int ac, char **av)
{
    int     i;

    while ((ret = getenv(name)))
    {
        printf("%s=%s\n", name, ret);
        name[4]++;
    }
    return (0);
}
